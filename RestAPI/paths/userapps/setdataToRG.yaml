put:
  tags:
  - userapp
  summary: Set Data to RG
  description: MQTT API :-  set_dataToRG <br> Sets data to RG.
  operationId: setDataToRG
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: string
            example: ''
  security:
  - bearerAuth: []
